#This file is generated by updateDaemonJvm
#Fabric Loom refuses to set up Minecraft 26.2 unless Gradle itself runs on Java 25,
#unlike ModDevGradle which is happy to compile through a Java toolchain. Declaring the
#daemon JVM here keeps that requirement portable instead of hard coding a local path.
toolchainVersion=25
